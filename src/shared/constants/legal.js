// Legal links surfaced in the app. Served by the backend; keep the URL here so
// it lives in one place (swap when you move to a custom domain).
export const PRIVACY_POLICY_URL = 'https://clean-pro-backend.onrender.com/privacy';
